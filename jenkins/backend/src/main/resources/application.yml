spring:
  datasource:
    url: jdbc:mysql://theramzee-mysql-1:3306/gradation_db
    username: root
    password : root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
  data:
    redis:
      port: 6379
      host: redis
  mail:
    host: smtp.gmail.com
    port: 587
    username: gd122572@gmail.com
    password: fzbujhthgsrmwhwh
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

jwt:
  secret: your_jwt_secret_key
  access-token:
    expiration: 360000000 # 1 hour in milliseconds
  refresh-token:
    expiration: 604800000 # 1 week in milliseconds

server:
  port: 8080

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html

openvidu:
  url: ${OPENVIDU_URL:https://ramzee.online:8443/}
  secret: ${OPENVIDU_SECRET:dleogus1}

logging:
  level:
    root: DEBUG                     # 루트 로거의 로그 레벨을 DEBUG로 설정
    com.gradation.backend: TRACE      # 특정 패키지 (com.gradation.backend) 의 로그 레벨을 TRACE로 설정
    org.springframework.security: DEBUG # Spring Security 관련 로그 레벨을 DEBUG로 설정