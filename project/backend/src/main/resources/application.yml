spring:
  datasource:
    url: jdbc:mysql://localhost:3306/gradation
    username: root
    password : root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
      dialect: org.hibernate.dialect.MySQLDialect
  data:
    redis:
      port: 6379
      host: localhost
  mail:
    host: smtp.gmail.com
    port: 587
    username: gd122572@gmail.com
    password: fzbujhthgsrmwhwh
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

jwt:
  secret: your_jwt_secret_key
  access-token:
    expiration: 3600000 # 1 hour in milliseconds
  refresh-token:
    expiration: 604800000 # 1 week in milliseconds

server:
  port: 5000

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html

openvidu:
  url: ${OPENVIDU_URL:http://d204test.p-e.kr:5443/}
  secret: ${OPENVIDU_SECRET:1234}
